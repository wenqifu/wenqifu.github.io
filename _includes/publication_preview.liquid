<div class="paper-gallery" role="group" aria-label="Figures from {{ include.title | escape }}">
  {% for slide in include.figures %}
    {% assign image_path = slide.image | prepend: '/assets/img/publication_preview/' %}
    <figure
      class="paper-slide"
      {% unless forloop.first %}
        hidden
      {% endunless %}
    >
      <a
        class="paper-figure-link"
        href="{{ image_path | relative_url }}"
        target="_blank"
        rel="noopener"
        aria-label="Open full-size figure: {{ slide.caption | escape }}"
      >
        {% if slide.image contains '.png' and slide.image != 'statz.png' %}
          {% assign responsive_base = image_path | remove: '.png' %}
          <picture>
            <source
              type="image/webp"
              srcset="{{ responsive_base | append: '-480.webp' | relative_url }} 480w, {{ responsive_base | append: '-800.webp' | relative_url }} 800w, {{ responsive_base | append: '-1400.webp' | relative_url }} 1400w"
              sizes="(min-width: 768px) 340px, 95vw"
            >
            <img class="preview" src="{{ image_path | relative_url }}" alt="{{ slide.caption | escape }}" loading="lazy" decoding="async">
          </picture>
        {% else %}
          <img class="preview" src="{{ image_path | relative_url }}" alt="{{ slide.caption | escape }}" loading="lazy" decoding="async">
        {% endif %}
      </a>
      <figcaption class="paper-caption">
        {% if slide.kind -%}
          <span class="preview-kind">{{ slide.kind }}</span>
        {%- endif -%}
        {{- slide.caption }}
      </figcaption>
    </figure>
  {% endfor %}
</div>
